# Track 1: Project Scaffolding

## Goal
Initialize the Next.js project with TailwindCSS, TypeScript, and basic directory structure.

## Tasks
1. [x] Initialize Next.js app using `create-next-app`.
2. [x] Set up basic folder structure (`src/components`, `src/lib`, `src/hooks`, `src/types`).
3. [x] Configure basic layout and global styles.
4. [x] Verify build and local dev server.

## Verification
- App runs on localhost.
- TailwindCSS is working.
- TypeScript is configured correctly.
